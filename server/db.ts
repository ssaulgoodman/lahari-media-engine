/**
 * LEGACY — SQLite is no longer used. All database operations go through
 * server/database.ts (Supabase Postgres via REST).
 *
 * This file is kept temporarily as a stub so any lingering imports compile.
 * Remove once all references are verified clean.
 */
console.warn('[db.ts] SQLite stub loaded — this should not be imported anymore. Use database.ts instead.');
export default null as any;
